Things to do inside the main build function:
    1. print all the messages(or max 10 messages and add more if user scrolls up)
    2. Make the ask button send a questions to the chat
    3. add this message to the database
    
    


usrmsg
Text(
                this.name,
                style: GoogleFonts.montserrat(
                  fontSize: 15,
                  fontWeight: FontWeight.w500,
                  color: Color(0xffeeeeee),
                ),
              ),
              
              
              
              
              
              
botmsg
Text(
                this.name,
                style: GoogleFonts.montserrat(
                  fontSize: 15,
                  fontWeight: FontWeight.w500,
                  color: Color(0xffeeeeee),
                ),
              ),